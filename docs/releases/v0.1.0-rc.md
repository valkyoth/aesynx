# Aesynx v0.1.0 Release Candidate Report

Status: implementation complete, tag blocked pending pentest

Commit under review:

```text
PENDING: set to the exact commit after this report is committed.
```

## Scope

`v0.1.0` is the repository foundation release. It does not claim a bootable
kernel, QEMU image, userspace runtime, hardware support, or production security.

The release includes:

- Rust workspace foundation.
- Stable Rust `1.96.0` toolchain pin.
- EUPL-1.2 license.
- Security policy.
- Threat model.
- Unsafe policy.
- Modularity policy.
- Kernel engineering policy.
- Release pentest gate.
- Dependency, advisory, and source policy.
- Initial no-std workspace crates.
- `xtask` scaffold.

## Completed Criteria

- Root `Cargo.toml` exists.
- `rust-toolchain.toml` exists.
- Workspace resolver is `3`.
- README identifies Aesynx and the 1.0 QEMU target.
- Implementation and release plans exist.
- Unsafe, modularity, threat-model, and kernel engineering policies exist.
- Workspace crates are declared and compile on the host.
- Local checks are wired through `scripts/checks.sh`.
- `cargo deny check` passes.
- `cargo audit` passes.
- SBOM generation works through `scripts/generate-sbom.sh`.

## Required Verification

Run before pentest:

```bash
scripts/checks.sh
scripts/generate-sbom.sh
cargo xtask build-kernel
```

Run after pentest report is written:

```bash
cargo xtask release-ready v0.1.0
```

## Known Limitations

- No bootable image yet.
- No QEMU run target yet.
- No custom-target kernel binary yet.
- No hardware interaction.
- No userspace execution.
- No external dependency exceptions currently exist.
- `v0.2.0` build skeleton work already exists on `main`, so the `v0.1.0`
  report describes the foundation criteria rather than an isolated branch.

## Tag Blocker

Do not tag `v0.1.0` until `security/pentest/v0.1.0.md` exists, names the exact
commit being tagged, and contains:

```text
Status: PASS
```

# Aesynx Security Controls

Status: baseline control map

This document maps the security posture Aesynx should grow into. It is not a
claim that the controls are implemented today.

| Area | Control | Current Status | Evidence |
| --- | --- | --- | --- |
| Project naming | Single project name, no retired names | Active | `scripts/validate-security-policy.sh` |
| Dependency policy | License, source, advisory, and duplicate-version checks | Configured | `deny.toml` |
| Security reporting | Private-first vulnerability process | Configured | `SECURITY.md` |
| Unsafe code | Unsafe confined to documented boundaries; new unsafe-bearing files and unsafe blocks without local `SAFETY:` comments block the security gate | Active and enforced | `docs/unsafe-policy.md`, `scripts/validate-security-policy.sh`, `crates/aesynx-arch-x86_64/src/port.rs`, `crates/aesynx-kernel/src/limine.rs`, `crates/aesynx-kernel/src/limine/abi.rs` |
| Kernel engineering | `no_std`, internal primitives, minimal unsafe, external dependency exceptions, `forbid` workspace lint gates for critical clippy checks, `#![forbid(unsafe_code)]` in unsafe-free crate roots, and documented `deny` plus narrow local unsafe exceptions only where hardware boundaries require them | Configured | `Cargo.toml`, `docs/kernel-engineering-policy.md`, `docs/unsafe-policy.md`, `scripts/validate-kernel-policy.sh` |
| Release overflow behavior | Release builds keep Rust integer overflow checks enabled and abort on panic, so future unchecked arithmetic regressions fail closed instead of silently wrapping | Active | `[profile.release]` in `Cargo.toml` |
| Modularity | Focused crates/modules, no giant source files | Configured | `docs/modularity-policy.md`, `scripts/validate-modularity-policy.sh` |
| Componentization | System must not collapse into one huge OS binary; components remain independently versioned and rollback-capable | Policy active | `docs/modularity-policy.md`, `docs/ARCHITECTURE_DECISIONS.md` |
| Supply chain | Executable dependency and workflow changes require review | Policy only | `docs/supply-chain-security.md` |
| Post-quantum readiness | Boot, package, update, entitlement, and identity metadata must stay crypto-agile and support future hybrid validation | Planned | `docs/post-quantum-readiness.md`, `docs/ARCHITECTURE_DECISIONS.md` |
| Static analysis | GitHub CodeQL default setup for Rust | Configured externally | GitHub code scanning default setup |
| Formal models and fault injection | Authority-bearing fabric work must grow concrete TLA+/Quint models, Kani/Verus proofs, Loom SPSC models, architecture ordering litmus tests, cross-endian golden vectors, decoder fuzzing, lock-rank/IRQ-context runtime checks, differential/metamorphic BootInfo tests, global W^X alias property tests, refinement tests, explicit safety/liveness/fairness properties, and fault injection for dropped/reordered messages, lost IPIs, coordinator death, AP late arrival, TLB-ack loss, full queues, and epoch exhaustion; model obligations are attached to the implementation milestones they guard, while v0.37.12 remains the integration/refinement gate | Planned | `docs/RELEASE_PLAN.md` v0.37.4, v0.37.8, v0.37.9, v0.37.11, v0.37.12, `docs/multikernel-fabric-roadmap.md` |
| Protocol specification ordering | Transactional grant, live queue publication/reuse, strong revocation, and AP restart/hotplug fencing require TLA+/Quint specifications before their live mechanisms can claim completion; v0.37.12 remains the later implementation conformance and fault-injection gate | Planned | `docs/RELEASE_PLAN.md` v0.37.0.1, v0.37.12 |
| Release pentest | Passing pentest report with exact tag, current commit, required metadata, matching release notes, generated SBOM evidence, ignored root scratch findings file, no live scratch findings file, and no existing local tag required before every tag | Configured | `scripts/validate-release-readiness.sh`, `scripts/validate-security-policy.sh`, `security/pentest/README.md` |
| Capabilities | No ambient authority as design center | Model active | `crates/aesynx-cap`, `docs/IMPLEMENTATION_PLAN.md` |
| Capability audit | Root mint, derive, grant, cross-table grant, and table revoke authority movement must use audited call paths once an audit sink exists; unaudited root insert is documented as bootstrap-only before audit setup; audit event debug output is redacted to action, permission bits, cross-owner status, and affected slot count; authenticated table call paths must validate against an external live authority view before audit emission instead of self-checking stored capability generation/epoch values; audit sinks must not reenter the table they are auditing; QEMU proves mint/derive/grant/revoke audit coverage before `[TEST] cap-audit=ok` and cap-IPC grant audit before `[TEST] cap-ipc=ok` | Active candidate | `CapAuditLog`, `LiveAuthorityView`, `CapabilityTable::insert_root_with_audit`, `derive_live_with_audit`, `grant_live_with_audit`, `grant_to_table_with_audit`, `CapabilityTable::revoke_with_audit`, `docs/releases/v0.25.0-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.37.0-rc.md` |
| Capability unforgeability | Capability fields are private, capability values are not `Copy` or `Clone`, external `CapId` values use a checked slot/nonzero-generation layout, production root minting rejects zero object generations before insertion or audit emission, bounded derivation requests require an `ObjectBoundedRange` checked against the real backing extent before the cap layer reduces authority, fixed-capacity kernel tables reject stale slot generations, derive/grant paths preflight table capacity before audit emission, cross-owner derivation and grant strip `GRANT`, `REVOKE`, and `ADMIN` from child capabilities, revoke authority requires `REVOKE` plus live generation/epoch validation before epoch mutation, table revoke is audited before mutation and total for every in-table capability on the target object including the authority cap, slot generations fail instead of wrapping at `u32::MAX`, capability debug output redacts authority identifiers plus permission/generation/epoch state, and table/audit debug output redacts authority identifiers; current capabilities carry authority metadata but no secret key material, and any future secret-bearing capability variant must explicitly scrub slot storage on revoke/delete | Active candidate | `aesynx-cap`, `docs/releases/v0.24.0-rc.md`, `docs/releases/v0.28.0-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.35.0-rc.md` |
| Authority identity hardening | Future hostile-boundary enforcement must use registry-minted stable object handles, object incarnations that cannot migrate between slots and resurrect stale capabilities, non-forgeable execution contexts for caller identity, domain-bound capability tables, table/domain/endpoint/boot-session incarnations, registry-issued mint tickets, endpoint rights, transactional grant commit/abort, a bounded one-writer preallocated transaction journal with torn-record integrity, witness/replication requirements, terminal-record reclamation, and explicit quarantine/resource-loss behavior when commit may have been observed but all trusted decision evidence is lost, coordinator-owned move-grant escrow with a named commit linearization point, one-shot reply capabilities with server-death cleanup, commit-time live revalidation or registry-visible lease semantics, replay windows, bounded stale-entry reclamation, versioned typed capability-kind permission matrices with separate common/kind attenuation, central kind-to-right metadata for domain lifecycle, task lifecycle, task join/result, per-kind parent-object derivation operations and internal derivation permits, debug, exception, pager, scheduling-context, and clock rights, generic cross-object dependency edges for derived child objects with internal one-shot derivation-control permits consumed at the parent-owned journal commit transition, local child publication after transaction commit, owner-local transaction-bound reservation manifests from versioned canonical required-reservation plans with parent-local preparing records before remote side effects, terminal ResourceLost tombstones, audit placeholders bound to commit and parent-owner-local in v1, deterministic reservation priority, single-parent v1 publication and future ParentSetManifest gating, authority/provenance separation, immutable relation policies, reserved revocation progress, distributed owner recovery, bounded strong-revocation worklists, and concurrency-safe cycle checks, high-risk rights nondelegable by default, `ADMIN` constrained to explicit audited operation IDs rather than acting as an override right, non-authoritative `CapId` tags, per-principal quotas, confused-deputy prevention for every service/RPC schema, and checked live proof types; caller identity and scheduling-budget donation are never substitutes for explicit capability arguments, nested service calls propagate only delegated authority, and service caches preserve caller authority/classification context; current caller-supplied `CoreId`/owner values and caller-selected object IDs remain scaffolding until v0.37.1 replaces them in enforcement APIs | Planned | `docs/RELEASE_PLAN.md` v0.37.1, `docs/IMPLEMENTATION_PLAN.md`, `docs/multikernel-fabric-roadmap.md` |
| Memory capabilities | Memory mapping requests require a `Memory` capability whose bounded range contains the requested mapping and whose permissions include `MAP` plus the requested READ/WRITE/EXECUTE access; unbounded memory roots can be used as kernel-internal derivation roots but cannot directly authorize mapping; QEMU verifies allowed read-only mapping-descriptor construction, missing READ denial, missing WRITE denial, and subrange escape denial before `[TEST] memory-cap=ok` | Active candidate | `aesynx-cap::MemoryMapRequest`, `aesynx-kernel::capability_smoke`, `cargo xtask qemu`, `docs/releases/v0.24.0-rc.md` |
| Capability-based shared memory | Future zero-copy sharing must use shared-buffer/object capabilities rather than raw physical-frame authority: v0.37.2 is descriptor/model-only, v0.37.10 is kernel-owned live mapping infrastructure with real page-table roots and CR3/TLB evidence, and hostile user-domain shared mappings are deferred until v0.46.1 after actual isolated ring-3 domains exist; each dispatcher receives a derived mapping capability, read-only sealed buffers are preferred for large assets, writable shared windows require `SHARE_WRITE`, a named synchronization protocol, audit events, revocation epoch integration, TLB shootdown handling, and mapper support that distinguishes declared shared-buffer aliasing from accidental physical-frame double ownership; writable cross-domain memory must be exposed as atomics, volatile bytes, or audited wrappers rather than safe aliased mutable Rust references | Planned | `docs/RELEASE_PLAN.md` v0.37.2, v0.37.10, v0.46.1, `docs/IMPLEMENTATION_PLAN.md`, `docs/concurrency-policy.md` |
| Memory authority scope | Raw physical frame allocation should stay owner-local/per-core where possible; capabilities govern memory objects, mapping authority, sharing, DMA visibility, ownership transfer, executable permission, and revocation rather than making every hot frame allocation/free a global agreement protocol; production mapping must consume checked memory-object plus address-space authority and cannot report permission reduction/unmap success until required TLB invalidation obligations are satisfied; W^X, cache attributes, and executable transitions are memory-object-wide alias invariants, page-table pages are protected from user, DMA, and ordinary writable mappings after installation, and post-install page-table updates require a narrow owner-core edit window or dedicated temporary mapping protocol with audit and restoration evidence; backing-frame lifecycle separates mutability (`Mutable`/`SealedReadOnly`), authority (`Live`/`Revoking`/`Revoked`), residency (`Mapped`/`Unmapping`/`Reclaimable`/`Dead`), and resource pins, with owner-core pin acquisition atomic against revocation, no usable reference publication before an owner-recorded live pin exists, an explicit legal lifecycle-state table, and no frame reuse until mappings, TLB obligations, DMA/IOMMU records, leases, queues, transactions, page-table edits, executable transitions, snapshots, and persistent references are drained and stale observers are fenced | Planned | `docs/memory-model-roadmap.md`, `docs/multikernel-fabric-roadmap.md`, `docs/RELEASE_PLAN.md` v0.37.7, v0.37.9, v0.37.10 |
| Multikernel fabric protocol | Future cross-core/domain communication must use a versioned bounded fabric protocol with explicit peer roles, sender/receiver identities, sequence/epoch fields, endianness and alignment rules, rejection/dead-letter behavior, backpressure, redacted diagnostics, downgrade-resistant version negotiation bound to peer/domain incarnations, required/optional extension semantics, canonical wire encoding, negotiated feature audit evidence, kernel-managed channel/session objects carrying negotiated protocol ID/version/feature hash/transcript, and no Rust-specific layout crossing heterogeneous boundaries unless the ABI/trust domain is proven identical | Planned | `docs/multikernel-fabric-roadmap.md`, `docs/RELEASE_PLAN.md` v0.37.3, `docs/concurrency-policy.md` |
| Replicated authority state | Future global authority changes such as capability revoke, service-owner transfer, routing policy, quota-credit transfer, and system policy updates must use owner/coordinator rules, coordinator incarnations and fencing tokens, epoch-stamped participant-set snapshots, monotonic epochs, bounded pending transaction counts, preallocated transaction journals, prepare/commit/abort or equivalent agreement, coordinator-failure recovery, timeout handling, stale-epoch fail-closed behavior, idempotent duplicate commit/abort, system-reserved non-delegable emergency audit-loss handling for revocation/quarantine/permission reduction, and linked audit records rather than hidden shared globals; escrowed quota credits cannot be reclaimed from dead or quarantined cores until old incarnations are fenced from committing further spends, and unfenced credits remain unavailable | Planned | `docs/multikernel-fabric-roadmap.md`, `docs/RELEASE_PLAN.md` v0.37.1, v0.37.4, `docs/concurrency-policy.md` |
| Topology-aware fabric routing | Future fabric routing must use deterministic policy over bounded topology facts such as peer role, core cluster, NUMA node where available, device locality, queue depth, recent latency, health state, and backpressure; AI may advise later only behind a non-AI fallback, and diagnostics must report route reason codes rather than raw addresses | Planned | `docs/multikernel-fabric-roadmap.md`, `docs/RELEASE_PLAN.md` v0.37.5, `docs/concurrency-policy.md` |
| Cross-core time semantics | Future fabric transactions, endpoint deadlines, leases, retries, watchdogs, coordinator timeouts, and AI advice expiry must not compare raw timestamps from different cores without a synchronized-clock capability and documented skew bound; protocols prefer relative TTLs, receiver-local deadlines, coordinator-local timeout decisions, clock generations/incarnations, independent watchdog sources where required, and fail-closed behavior for backward jumps, unstable calibration, epoch changes, suspend/resume, AP restart, VM migration, TSC instability, and counter rollover; timeouts may abort/quarantine/retry but never manufacture commits | Planned | `docs/RELEASE_PLAN.md` v0.37.3, v0.37.3.1, `docs/multikernel-fabric-roadmap.md` |
| Fabric fault containment | Future driver/service domains must have heartbeats, watchdogs, quarantine, revoke-on-fault, in-flight message cancellation or replay rules, DMA/IOMMU cleanup before restart, service rebinding, restart budgets, escalation policy, and telemetry before failure containment is claimed | Planned | `docs/multikernel-fabric-roadmap.md`, `docs/RELEASE_PLAN.md` v0.37.6, `docs/concurrency-policy.md` |
| Minimal ring-0 TCB | Future production Aesynx should converge on a per-core CPU-driver-style privileged component for local traps, protection, address-space switching, interrupts, and message delivery; global capability agreement, topology/world queries, telemetry aggregation, package policy, driver policy, and AI/model execution belong in isolated monitor/service domains, while current in-kernel smokes remain scaffolding until userspace exists; v0.37.7 also requires measurable TCB boundaries: privileged dependency allowlists, decoder-surface limits, static-memory budgets, ring-0 heap inventory, unsafe-island ownership, extraction targets for policy scaffolds, and monitor-failure tests | Planned | `docs/multikernel-fabric-roadmap.md`, `docs/os-world-roadmap.md`, `docs/RELEASE_PLAN.md` v0.37.7 |
| Minimum core incarnation contract | Live AP-backed IPC consumes machine boot/session nonce, topology epoch, immutable core incarnation, startup-attempt generation, endpoint incarnation, and link generation; failed or timed-out APs stay quarantined until reboot and identities are not reused before the richer restart/hotplug model exists | Planned | `docs/RELEASE_PLAN.md` v0.37.7.1 |
| Per-core memory ownership | Production multicore allocation uses owner-stamped frames, per-core heap/slab ownership, remote-free queues, explicit frame-transfer transactions, emergency reserves, and quarantine recovery; no core directly mutates another core allocator's metadata, and frames cannot be reused while remote references or revocation/TLB/DMA obligations remain live; remote frees remain pending until the owning allocator acknowledges them, full queues use bounded quarantine/retry/backpressure rather than dropping frees, metadata carries frame/allocation/owner incarnation plus transaction ID, ordinary callers cannot consume emergency capacity, and dead or quarantined owner frames remain unavailable until fenced/reset or halt | Planned | `docs/RELEASE_PLAN.md` v0.37.7.2 |
| Live atomic fabric queues | Future AP-backed IPC must replace sequential model queues with cache-aware atomic SPSC links: single-writer monotonic cursors, no shared hot-path length field, release/acquire slot publication, reverse release/acquire acknowledgement before producer scrub/reuse, named enqueue/dequeue/cancel/reuse linearization points, slot generation for reuse, one-writer page ownership for payload/publication versus acknowledgement/cursor pages, scrub-on-vacate, cache-line/page separation for endpoint metadata, sparse link creation, direct-link thresholds, hierarchical doorbells, IPI coalescing, kernel-stamped capability-derived traffic classes with reserved control capacity and rate limits, per-principal credits, bounded retry/cancellation/dead-letter handling, and either fixed-width atomic wire frames or a tiny audited unsafe queue-storage island; queue endpoints name exact writer context, reject reentrant and panic-recursive mutation, keep NMI/MCE paths off ordinary queues, publish payloads before doorbells with architecture barriers, use no-lost-wakeup sleep handshakes, and treat doorbells as hints while queue state remains authoritative; liveness requires a persistent pending bit, bounded retransmission, receiver watchdog/polling, level-triggered notification, or deep-idle denial, with acknowledgements bound to doorbell generation, link incarnation, receiver incarnation, and observed cursor; AP restart/hotplug stay prohibited until incarnation fencing exists; hostile userspace submission queues use trusted kernel shadow cursors, treat all user queue metadata as hostile, perform one fault-contained copy into owned storage, and validate that attacker-controlled snapshot rather than trusting user-controlled generation counters to detect mutation | Planned | `docs/RELEASE_PLAN.md` v0.37.8, v0.37.11, v0.45.1, `docs/multikernel-fabric-roadmap.md` |
| Fabric link lifecycle | Sparse fabric links require Absent/Creating/Active/Draining/Retired/Reclaimable state, stopped publication before teardown, drained/cancelled messages, retired doorbells/vectors, invalidated endpoint/link generations, TLB-completed unmaps, payload scrubbing, stale IPI/ack rejection, and distributed no-deadlock protocol ranks before link reuse | Planned | `docs/RELEASE_PLAN.md` v0.37.8.1 |
| Strong revocation | Future strong revocation must freeze authority, tear down mappings, complete local and remote TLB invalidations with acknowledgements bound to address-space incarnation, ASID/PCID generation, mapping generation, virtual range, operation class, target-core incarnation, and revocation transaction ID, quiesce DMA, resolve in-flight IPC, handle coordinator death through recovery/abort/quarantine, and commit or abort through an auditable distributed protocol; prospective revocation linearizes only after every authorizing core has installed a deny/freeze fence or been fenced/quarantined/reset, cached proofs are invalidated or registry-visible leases, and new proof issuance is blocked; revocation/quarantine/permission reduction cannot be blocked solely by normal audit-buffer exhaustion, selective revocation must carry concrete lineage-node identity/generation, parent/domain links, bounded depth/children, indexed mappings/DMA/leases/queues/transactions, and tested reuse/exhaustion semantics for one-entry, subtree, domain, or object-wide invalidation, and an unresponsive-but-still-running core with stale TLB reachability prevents success unless hardware-reset, fenced from execution, or the system halts; local epoch caches remain advisory and simple epoch bumps are not sufficient liveness evidence unless backed by a current owner-issued lease or installed fence | Planned | `docs/RELEASE_PLAN.md` v0.37.9, `docs/multikernel-fabric-roadmap.md`, `docs/memory-model-roadmap.md` |
| Tagged TLB and AP restart gates | Initial x86_64 live mapping uses PCID disabled, PCID metadata fixed to zero, full non-global CR3 flushes, explicit `invlpg`/full-context flush policy, CR4.PGE/global mappings disabled, and an address-space residency barrier enforced through a non-forgeable activation permit that binds address-space incarnation, mapping generation, and residency epoch before context switch; PCID/INVPCID and actual AP restart/hotplug remain disabled until a separate live-evidence gate proves allocator/reuse-generation/rollover/invalidation behavior and real restart rejection of old messages/endpoints/table IDs/topology snapshots | Planned | `docs/RELEASE_PLAN.md` v0.37.10, v0.37.11, v0.37.13 |
| Object graph model | Host-side object graph rules reject zero object IDs, redact object ID debug output, reject self-references and duplicate references before graph insertion, require referenced/predecessor objects to already exist, and compute reachability over immutable references plus predecessor links | Tagged | `models/aesynx-object-model`, `docs/releases/v0.23.0-rc.md` |
| Kernel object registry | Kernel-facing local object registry rejects zero object IDs, duplicate live IDs, and over-capacity insertion; deleted slots are recycled only after checked generation increment so stale capabilities fail closed if an object ID is recreated; generation-exhausted slots remain live and must be surfaced through future diagnostics/metrics as a registry-retirement signal so operators can detect capacity erosion; list operations validate output capacity before writing; object records redact object IDs in debug output; capability resolution requires a live object with matching object ID, object kind, object generation, object revocation epoch, and requested permission; the registry implements the revocation epoch store so a checked epoch bump invalidates stale capabilities held in other tables; larger syscall-reachable registries need an indexed lookup path or strict per-owner quotas before this becomes a hot authority check | Active candidate | `crates/aesynx-object`, `docs/releases/v0.24.0-rc.md`, `docs/releases/v0.25.0-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.37.0-rc.md` |
| Kernel service queues | Kernel-facing no_std service queues reject zero message IDs and zero-capacity queues, preserve FIFO ordering across ring wraparound, fail full pushes and empty pops before mutating queue state, route only log/timer/object skeleton services in v0.26, reject every currently unsupported service kind before queue mutation and for pending-count inspection instead of reporting a false zero backlog, require typed live-core validation before message-header destination routing, require the recorded owner core before push/pop/completion/pending-count operations and reject non-owner callers before mutation, record release/acquire publish-observe ordering evidence, redact request/completion payloads and authority-bearing identifiers in debug output, and QEMU-smoke a log request/completion plus unsupported-service denial through `[TEST] service-queue=ok`; future shared-memory or multi-core queues must scrub vacated payload storage before cross-domain observation and must prove the documented release/acquire pair on actual atomic head/tail or slot-validity updates, not only on metadata evidence | Tagged | `crates/aesynx-ipc`, `crates/aesynx-kernel::service_queue_smoke`, `models/aesynx-ipc-model`, `docs/releases/v0.26.0-rc.md`, `docs/releases/v0.28.0-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.35.0-rc.md` |
| Pairwise IPC fabric | Core-to-core IPC now has a no_std pairwise SPSC fabric model with explicit producer and consumer core identities, live-core validated endpoints, route validation against kernel-stamped message headers, non-`Copy` ping/pong state so the sequence allocator cannot be forked, sequence numbers that commit only after successful enqueue, correlated Ping/Pong reply checks, release/acquire ordering evidence, fail-closed bidirectional full-queue backpressure, capacity-generic queue draining on backpressure proof paths, a documented future slot-scrub requirement before sensitive payloads cross trust domains, and QEMU evidence through `ipc-pingpong ping_seq=`, `ipc_backpressure_ok=true`, `ipc_release_acquire_ok=true`, `ipc_pairwise_route_ok=true`, and `[TEST] ipc-pingpong=ok`; this remains a model-backed smoke path until hardware-backed atomic queue slots and AP-backed doorbell/IPI delivery land in v0.37.8 | Tagged | `crates/aesynx-ipc::fabric`, `crates/aesynx-kernel::ipc_pingpong_smoke`, `docs/releases/v0.36.0-rc.md`, `docs/RELEASE_PLAN.md` v0.36.0 |
| Capability grant over IPC | Sender tables can grant into a receiver capability table only through audited cross-table allocation, live object generation/epoch validation, and source `GRANT` permission checks; QEMU sends the receiver-allocated `CapId` through a kernel-stamped `GrantCap` IPC message, proves receiver WRITE denial and sender missing-GRANT denial, sends a `RevokeCap` notification, bumps the object registry revocation epoch, and proves the receiver cap resolves as revoked before `[TEST] cap-ipc=ok`; this is still model-backed and does not yet provide AP execution, user endpoints, or a production revoke broadcast protocol | Active candidate | `CapabilityTable::grant_to_table_with_audit`, `ObjectRegistry::increment_epoch`, `crates/aesynx-kernel::capability_ipc_smoke`, `docs/releases/v0.37.0-rc.md`, `docs/RELEASE_PLAN.md` v0.37.0 |
| Kernel task model | Kernel-facing no_std task objects expose read-only identity/scheduling fields, are deliberately non-`Copy`/non-`Clone` so one logical task cannot be duplicated across queues, use checked state transitions, redact task IDs in debug output, keep run/wait queues non-`Sync` until a future lock wrapper provides the synchronization contract, return rejected task ownership on failed queue admission, reject zero task IDs at queue admission, reject wrong-core runnable tasks, preserve local run-queue FIFO order, reject duplicate/full/empty queue operations before mutation, restore wait-queue state on failed wake transitions, and wake matching wait-queue entries back to runnable state before `[TEST] task-model=ok`; large or syscall-hot queues require indexed membership tracking, and live scheduler queues require local interrupt/preemption protection before queue mutation | Tagged | `crates/aesynx-sched`, `crates/aesynx-kernel::task_smoke`, `docs/releases/v0.27.0-rc.md` |
| Cooperative executor | Kernel-facing no_std local executor dispatches runnable tasks through deterministic round-robin order, rejects nested dispatch before mutation, preserves linear task ownership across failed yield/sleep/wake reinsertion paths, preserves rejected tasks inside restore-failure errors while redacting the carried task from `ExecutorError` debug output, preflights timer sleep/wake queue capacity and duplicate checks before state movement, checks dispatch/yield/sleep/wake counter increments before moving tasks so counter-overflow errors are atomic, and QEMU-smokes an A/B/A/B interleaving with one timer wait/wake before `[TEST] cooperative-sched=ok`; architectural context switching, preemption, userspace tasks, SMP sharing, and IRQ-safe live executor locking remain future gates | Tagged | `crates/aesynx-sched::executor`, `crates/aesynx-kernel::cooperative_sched_smoke`, `docs/releases/v0.28.0-rc.md`, `docs/releases/v0.32.0-rc.md` |
| Preemptive scheduling | Future multi-process userspace and metered AI policy services require timer-driven forced kernel entry, full architectural context save/restore, per-core scheduler ownership, checked quantum and CPU-budget accounting, preemption-disable nesting, IRQ/preemption-safe run-queue mutation, watchdog termination or reset for non-yielding domains, and an infinite-loop user-task QEMU proof that other tasks still run | Planned | `docs/RELEASE_PLAN.md` v0.46.2 |
| Scheduling contexts | Future CALL/REPLY and service execution must use explicit scheduling-context and budget-donation rules so clients cannot amplify work through privileged services; scheduling-context rights include donate, set-ceiling, cancel-donation, and inspect-budget, with set-ceiling nondelegable by default; donated budget is bounded, transaction-scoped, priority-capped, depth-limited, returned on reply/cancel/timeout/death, and kernel parsing, copying, capability lookup, page faults, IRQ work, and async messages are either charged to a principal/service budget or strictly bounded | Planned | `docs/RELEASE_PLAN.md` v0.46.2.1 |
| Transactional task migration | Cross-core task migration must freeze source ownership, stage destination ownership, transfer architectural state, address-space activation permit, timers, IPC wait/reply state, budgets, accounting, affinity/security constraints, and outstanding references, then commit exactly one runnable owner or abort back to the source; pinned IRQ/device/control tasks cannot migrate | Planned | `docs/RELEASE_PLAN.md` v0.46.3 |
| Scheduler telemetry | Scheduler decisions can be recorded in a bounded no_std trace buffer with typed core/task IDs, redacted decision debug output, checked capacity, deterministic round-robin decision reasons, explicit queue-depth saturation flags, preflighted telemetry capacity before telemetry-aware dispatch mutates executor state, per-task scheduled-run counters, and QEMU smoke evidence before `[TEST] scheduler-telemetry=ok`; core IDs are intentionally visible as non-secret local scheduling context until SMP tenancy becomes a boundary, and traces are local advisory facts rather than coherent cross-core scheduler transactions | Tagged | `aesynx-telemetry::SchedulerTelemetry`, `LocalExecutor::dispatch_next_with_telemetry`, `crates/aesynx-kernel::scheduler_telemetry_smoke`, `docs/releases/v0.29.0-rc.md` |
| Telemetry availability boundary | Current telemetry-aware dispatch capacity preflight is scaffold-only; production scheduling, IPC, revocation, and fault handling must not depend on noncritical telemetry availability. Advisory telemetry drops or overwrites with loss counters, while security audit uses reserved fail-closed channels only for authority-expanding operations | Planned | `docs/RELEASE_PLAN.md` v0.61.2 |
| Telemetry event schema | Kernel facts can be recorded as versioned no_std telemetry events with explicit event IDs, kernel-stamped headers, monotonic per-core sequence numbers, typed boot-phase/capability-fault/scheduler-decision payloads, bounded append-only per-core event storage, aggregate summaries, scheduler selected-task debug redaction, and QEMU smoke evidence before `[TEST] telemetry-events=ok`; event rings are local unsynchronized buffers and are not yet a stable userspace ABI, persistent trace format, IPC export format, or coherent cross-core transaction log | Tagged | `aesynx-telemetry::PerCoreEventRing`, `TelemetryEventHeader`, `TelemetryEventPayload`, `crates/aesynx-kernel::telemetry_events_smoke`, `docs/releases/v0.30.0-rc.md` |
| Trace export | Schema-v1 serial `trace-event` records can be decoded offline by `tools/trace-decode`; export is line-based, rejects unknown schema/events/fields, keeps CoreId visible as local non-secret scheduling context until SMP tenancy becomes a boundary, and rejects scheduler events unless selected task identity is already `<redacted>` | Tagged | `tools/trace-decode`, `cargo xtask trace-decode`, `docs/telemetry-event-schema.md`, `docs/releases/v0.31.0-rc.md` |
| AI policy interface | AI policy is advisory only: model manifests require schema-v1 metadata, nonzero hash/signature metadata presence, matching policy domain, bounded resource limits, required fallback, scheduler telemetry fields, bounded confidence, fixed-point scheduler feature validation for every non-priority feature, fixed-point-only accepted model kinds, redacted manifest debug output, structural-only validated-manifest documentation, deterministic zero-confidence fallback, bounded non-AI scheduler heuristic scoring, decision records, a disable switch that preserves the local round-robin fallback path, fail-closed invalid-feature handling, and redacted scheduler heuristic score/core serial evidence before any model can influence later policy work; hash/signature-bearing manifest types intentionally do not implement equality so they cannot become an accidental authentication fast path; model-backed decisions must use the manifest-aware constructor that enforces `ModelSafetyLimits::max_confidence`; presence-only hashes/signatures must not become a model execution trust boundary before real verification exists; no LLM/model runtime or rich AI scoring belongs in production ring 0, and current kernel smokes are scaffolding for deterministic policy shape until the advisory AI service can live in an isolated userspace domain; future AI policy execution must be asynchronous and metered, with v0.46.2 preemptive CPU enforcement before any cannot-lock-a-core claim, fuel/deadline/memory limits, runtime model authenticity checks, stale-advice expiry, topology/task incarnation validation, non-public `ValidatedScheduleAction` outputs, bounded kernel-side action validation, scheduler-independent deterministic fallback, an audit-buffer failure decision table, and telemetry-loss behavior that cannot block dispatch; QEMU proves accept/reject/fallback behavior with `manifest_metadata_gate_ok=true`, `heuristic_enabled=true`, `heuristic_score=<redacted>`, `heuristic_core=<redacted>`, and `heuristic_disabled_fallback_ok=true` before `[TEST] ai-policy=ok` | Tagged; async service planned | `aesynx-ai-policy`, `crates/aesynx-kernel::ai_policy_smoke`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.33.0-rc.md`, `docs/releases/v0.35.4-rc.md`, `docs/releases/v0.35.5-rc.md`, `docs/RELEASE_PLAN.md` v0.46.2, v0.61.2 |
| Model-loader trust lifecycle | Model bytes cannot influence advisory runtime policy until a real signature over canonical manifest/evaluator-consumed bytes validates against stored trust roots with key rotation, signer revocation, anti-rollback, policy-domain binding, parser downgrade rejection, and fail-closed behavior for unavailable clock/revocation/trust-root status | Planned | `docs/RELEASE_PLAN.md` v0.59.0 |
| AI scheduler validation | Model output is hostile input, never a scheduler action. Production scheduler mutation requires a non-public validated action produced by a total bounded validator over authoritative kernel state, followed by commit-time scheduler/topology epoch recheck; arbitrary model bytes must produce either a sealed valid action or the deterministic fallback | Planned | `docs/RELEASE_PLAN.md` v0.61.2 |
| Telemetry overwrite snapshots | Future overwriteable telemetry rings must use seqlock-style odd/in-progress and even/complete generations; readers copy records and accept only equal even generations, never persistent references into overwriteable slots, so userspace collectors cannot observe torn records as valid snapshots; generation wrap retires/reinitializes the ring before reuse and writer death while odd leaves a rejected slot plus recovery event until the slot is completed, retired, or reinitialized | Planned | `docs/RELEASE_PLAN.md` v0.61.2 |
| CI action integrity | Workflow actions must be pinned to commit SHA | Active | `scripts/validate-security-policy.sh`, `.github/workflows/ci.yml` |
| State transitions | Task and device state changes must use checked transition APIs; device `Revoked` and `Crashed` states are terminal sinks and cannot churn between terminal states after incident classification | Model active | `Task::transition`, `DeviceObject::transition`, `docs/releases/v0.32.0-rc.md` |
| Boot address secrecy | KASLR-sensitive kernel image, RSDP, HHDM, device-tree, framebuffer, and memory-region physical starts are private or debug-redacted; framebuffer base access goes through an explicit `BootInfo` accessor rather than public fields | Active candidate | `KernelImageInfo`, `FramebufferInfo`, `MemoryRegion`, `BootInfo`, `BootMetadata`, `cargo xtask qemu` |
| BootInfo normalization | Bootloader-specific handoff data normalizes before generic kernel use; Limine response pointers and payload addresses are null/alignment/higher-half checked with an explicit architecture VMA floor, memory-region validation rejects empty, overflowing, overlapping, and over-cap region slices before accounting, kernel-image virtual/physical bounds are page-aligned, and the kernel physical footprint must be fully covered by `Kernel`/`Reserved` memory-map entries without overlapping `Usable` memory before allocator work can trust it; request statics live in the RW handoff segment inside a private ABI module; the Limine entries-array allocation length behind `entry_count` remains an accepted bootloader trust boundary because the kernel can bound the count and validate each loaded pointer, but cannot prove the bootloader's backing array extent internally | Active candidate; bootloader array extent trusted | `aesynx-boot`, `crates/aesynx-kernel/src/limine.rs`, `crates/aesynx-kernel/src/limine/abi.rs`, `linker/kernel-x86_64.ld`, `docs/releases/v0.32.0-rc.md` |
| Firmware pointer provenance | Framebuffer and RSDP/ACPI handoff addresses must stay metadata-only until wrapped in typed provenance APIs with explicit read/write permissions and consumer-specific validation | Planned near-term | `docs/RELEASE_PLAN.md`, `docs/releases/v0.16.1-rc.md` |
| BootInfo fuzzing | Bootloader-shaped memory maps and metadata have bounded deterministic fuzz coverage before allocator or address-space activation code treats normalized BootInfo as trusted input | Active candidate | `cargo xtask fuzz-smoke`, `docs/RELEASE_PLAN.md` v0.16.1 |
| Physical memory accounting | Boot memory maps are checked for empty regions, region overflow, overlapping ranges, total/usable/reserved bytes, kind-specific reserved ranges, and full-frame counts before allocator work can trust them | Active candidate | `aesynx-boot::MemoryMap`, `cargo xtask qemu` |
| Frame allocation | Early bitmap allocator tracks known/free/used/reserved frame state, rejects overlapping seeds, detects double-free, validates region seeding and contiguous frees before committing bitmap mutations, rejects impossible private bitmap combinations on checked status reporting and before region marking/allocation/free mutations, keeps allocator status fields read-only through public accessors, keeps contiguous-allocation tokens allocator-produced with read-only accessors and redacted debug output, redacts allocator debug output to aggregate counters, avoids unchecked status arithmetic underflow, and QEMU-smokes the checked status path against a bounded usable boot-map window | Active candidate | `aesynx-mm::BitmapFrameAllocator`, `cargo xtask qemu` |
| Page-table mapping | Safe bounded mapper model validates canonical/aligned addresses, exposes an opaque typed non-physical checked root identity only after audit validation, keeps unchecked root and status reporting test-only, gates kernel address-space candidates through audit/root/status/non-empty/kernel-only/no-user-space/no-device/no-alias checks with a consolidated read-only shape walk, gates user address-space candidates through audit/root/status/non-empty/high-half-user/low-half-kernel/has-low-user/no-device/no-global/no-alias checks with a consolidated read-only shape walk, keeps candidate preflights read-only on success and failure, audits standalone mapping policy helpers before policy evaluation, rejects double maps, rejects duplicate physical-frame mappings at map time through a const-capacity bounded sorted frame index when exclusive ownership is required, audits side-index sortedness, table/index agreement, missing entries, stale entries, and duplicate table leaves with a bounded top-down root traversal instead of fixed-point rescans, accepts hardware-set x86_64 Accessed/Dirty leaf bits while keeping constructors clean, keeps access, privilege, device, and cache flags behind constructors, builders, and read-only accessors, keeps unchecked page-mapping descriptor construction and raw x86_64 entry encoding crate-private, audits single-page map/protect/unmap before mutating existing mapper state, keeps the mapper non-`Copy` so full arenas cannot be implicitly copied onto kernel stacks, commits contiguous map/protect/unmap in place with bounded prefix rollback rather than full-arena candidate copies, preserves the original range-operation error when rollback succeeds and reports `CorruptTable` only when rollback fails, maps/unmaps 4 KiB pages, exposes checked public page-mapping descriptor construction, routes single-page map/protect mapping creation through that checked constructor, exposes audit-backed fail-closed single-address and contiguous byte-range translation plus read-only permission lookup, page-presence reporting, range checks, callback-based mapping visits before emitting records, and aggregate mapping summaries while preserving mapper state on corrupt read-only range and range-policy failures, redacts address-space root and page-table debug output to aggregate counters, aggregate report counters, model-root wording, non-address fields, and private slot/range classifications, fails closed on malformed non-empty leaves, malformed non-empty raw slots, malformed next-table links, decoded root-table child links, dangling or out-of-range child links, invalid internal helper/table-path indices, invalid zero-page virtual-space/range-walk validation, empty-arena capacity/table-path checks, and invalid reclaim paths, validates reclaim spans and parent-child slot links before committing slot/table changes, checks mapped/unmapped/protected/kernel/user/executable/normal-memory/local ranges plus high-half kernel-space and low-half user-space ranges, rejects empty candidate address spaces, rejects kernel-only user candidates, rejects user-accessible mappings in the high-half kernel region, rejects kernel-privileged mappings in the low-half user region, rejects device mappings in candidate kernel address spaces, rejects device and global mappings in candidate user address spaces, changes mapping permissions without changing frames, reclaims empty intermediate tables, carries generic page flags, and returns explicit TLB flush targets before any live CR3 activation claim | Active candidate | `aesynx-mm::PageTableMapper`, `cargo xtask qemu`, `docs/releases/v0.32.0-rc.md` |
| Sealed hardware address-space export | Future hardware page-table export and CR3 installation must consume a non-forgeable sealed validated-address-space proof produced by complete policy validation; raw mapper arenas, partially checked roots, and advisory status reports are not sufficient to reach hardware export | Planned | `docs/RELEASE_PLAN.md` v0.16.2, v0.37.10 |
| Kernel mapping policy | Kernel text, rodata, data, reserved heap, guard page, and null-page ranges have a safe policy descriptor; normal boot derives image ranges from page-granular linker section symbols; safe host-tested planning rejects malformed section order, unaligned boundaries, low-half and noncanonical section ranges, empty heap/guard reservations, non-high-half reserved windows, and arithmetic overflow before policy creation; verification audits mapper state, rejects overlapping or overflowing policy ranges, requires text RX/write-protected, rodata read-only/NX, data RW/NX, normal local kernel mappings, rejects user/device/global violations on kernel image ranges, reports verifier-derived status fields, and requires heap/guard/null ranges to remain unmapped before reporting `[TEST] paging-policy-model=ok` | Active candidate | `aesynx-mm::KernelMappingPolicy`, `aesynx-kernel::kernel_mapping_policy`, `cargo xtask qemu` |
| Kernel-owned address space | Kernel mapping policy graduates to real Aesynx-owned page tables in the terminal v0.16.2 boot smoke: audited mapper state streams redacted x86_64 hardware-shaped tables using Limine's normalized kernel physical placement into a static activation arena, the kernel switches to a private activation stack, loads the Aesynx-owned CR3 root, and reports post-switch evidence before halting; general post-switch services and TLB/PCID policy remain future gates, while CPU hardening and stack-guard evidence advance in v0.16.3 | Tagged | `docs/RELEASE_PLAN.md` v0.16.2, `docs/releases/v0.16.2-rc.md` |
| Kernel PIE/KASLR | KASLR is defense-in-depth and must be implemented through PIE kernel code generation, audited relocation types, approved-DRBG slide selection, canonical/aligned slide constraints, collision checks against direct map/framebuffer/modules/heap/stacks/page tables/MMIO/firmware windows, BSP/AP slide agreement, relocation overflow rejection, redacted diagnostics, no public raw-slide leakage, and measured effective entropy; if entropy or relocation support is unavailable under a selected profile, Aesynx fails closed rather than treating static layout as equivalent | Planned | `docs/RELEASE_PLAN.md` v0.44.6 |
| CPU hardening bits | EFER.NXE, CR0.WP, and CPUID-gated SMEP/SMAP/UMIP are enabled after Aesynx-owned CR3 activation where supported, with NX required fail-closed, redacted boolean-only status output, and read-back verification that requested bits stuck; CPUID-gated Spectre-class controls now include Intel leaf-7 and AMD extended-leaf IBRS/IBPB/STIBP/SSBD detection plus `ARCH_CAPABILITIES` evidence, AMD extended IBRS detection uses bit 14 with regression coverage rejecting unrelated bit 9, `IA32_SPEC_CTRL` and `IA32_PRED_CMD` are admitted MSRs, supported IBRS/STIBP/SSBD bits are requested and read back where available, one-shot boot-time IBPB is attempted when supported, `wrmsr` is not marked `nomem`, serial output separates `ibpb_supported` from `ibpb_attempted`, strict-mode diagnostics distinguish missing IBRS from missing IBPB, and no per-context-switch IBPB or userspace Spectre-v2 isolation claim exists yet; normal QEMU builds use the default required plan while high-assurance builds can opt into the `strict-cpu-hardening` feature to fail closed unless NX, SMEP, SMAP, UMIP, IBRS/IBPB, STIBP, SSBD, and `ARCH_CAPABILITIES` are all available; remaining microarchitectural work such as ARCH_CAPABILITIES field decode, microcode-revision and mixed-core mitigation policy, KPTI/L1TF hygiene, RSB/BHB/BHI policy, VERW-based buffer clearing, retpoline/IBRS policy, and broader side-channel strategy remains planned | Active candidate; domain transition hardening planned | `docs/RELEASE_PLAN.md` v0.16.3, v0.35.1, v0.35.4, v0.44.5, v0.44.7, `docs/releases/v0.16.3-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.35.0-rc.md`, `docs/releases/v0.35.2-rc.md`, `docs/releases/v0.35.4-rc.md`, `docs/releases/v0.35.5-rc.md` |
| Domain-transition hardening sequence | Future hostile-domain entry must use an explicit boot and transition state machine: validate CPUID/deployment baseline, establish NXE/WP before relying on NX-bearing or read-only tables, activate verified CR3 through a current-stack-safe transition, enable SMEP/SMAP/UMIP only after access-window contracts exist, decode eIBRS versus legacy IBRS policy, handle context-switch IBPB, RSB/BHB/BHI, KPTI/L1TF, VERW-based buffer clearing, SMT sibling/core scheduling, per-task STIBP/SSBD, microcode-dependent mitigation validity, and PCID/INVPCID-aware isolation before claiming process isolation; syscall/user-return code must validate user RIP/RSP/RFLAGS, avoid unsafe SYSRET, run CLD/CLAC/SWAPGS fencing as required, normalize exception frames, scrub sensitive scratch registers, contain entry/exit faults, and test hostile register/NMI-around-SWAPGS cases; critical entry/exit assembly requires disassembly checks, shared Rust/assembly frame-layout assertions, validation-funnel checks, and fault injection at sensitive phases | Planned | `docs/RELEASE_PLAN.md` v0.44.5 |
| Side-channel isolation profile | Optional high-assurance profiles must name enforceable side-channel controls rather than broad claims: SMT sibling isolation or SMT disablement, security-domain-aware placement, capability-gated high-resolution clocks/performance counters, quantized telemetry, cache/LLC and memory-bandwidth partitioning where supported, measured page coloring only when included in allocator policy, predictor/TLB/buffer-clearing transition policy, recorded covert-channel edges for shared buffers/cache/core/device relationships, and fail-closed behavior when requested isolation cannot be enforced | Planned | `docs/RELEASE_PLAN.md` v0.44.7 |
| Kernel stack guards | The terminal activation stack is placed in a dedicated linker section, mapped explicitly as RW/NX kernel memory, and preceded by an unmapped guard page before Aesynx loads the active CR3 root; broader per-core/per-task stack guards remain future work, and the current static double-fault IST stack is not guard-paged until per-core IST mappings exist | Tagged; IST guard planned | `docs/RELEASE_PLAN.md` v0.16.3, `docs/releases/v0.16.3-rc.md` |
| Kernel heap | The kernel heap is a bounded page-aligned static allocator initialized only after Aesynx-owned CR3 activation and CPU hardening; fixed slab classes cover small allocations, page-sized runs cover larger allocations, metadata mutation uses an IRQ-masked lock on freestanding x86_64, rejects reentrant acquisition instead of spinning forever, and restores the previous interrupt state on success and rejected acquisition paths; checked deallocation detects invalid frees and free-while-free double frees, production `GlobalAlloc::dealloc` fail-stops on invalid free, double free, corrupt free-list, or reentrant lock detection, aggregate telemetry separately surfaces invalid-free attempts, allocation-accounting overflow, and corrupt free-list detections, release-checked free-list offset decoding rejects corrupt sentinels without relying on overflow checks, slab allocation pops, double-free membership walks, and slab-page reclaim validate free-list offsets for heap bounds, slab-block alignment, and bounded walk length before pointer dereference, free-list scans are bounded by cached actual slab pages for the class rather than total heap bytes or a full page-state walk, slab page count release is checked so underflow fails closed with corruption telemetry instead of wrapping, allocation/free accounting uses checked compare-exchange loops and records overflow/underflow telemetry instead of panicking or silently saturating, slab and page-run memory is zeroed before first return and before reuse, slab page live counters avoid normal free-list full walks, helper-module field visibility remains confined to `kernel_heap` internals for allocator implementation splitting, and QEMU smokes `Box`, `Vec`, `BTreeMap`, slab reuse, page-run allocation/free, stress allocation/free, invalid-free telemetry, double-free detection, accounting-overflow telemetry host coverage, corruption telemetry clear-state reporting, and explicit oversized allocation rejection before `[TEST] heap=ok`; allocation-epoch stale raw-pointer detection, O(1) slab free-list membership tracking, O(1) free-page/run tracking, physical-frame-backed growth, per-core heaps, non-`static mut` backing storage, and bounded IRQ-masked latency before material heap growth remain future work | Active candidate; per-core heap planned | `crates/aesynx-kernel/src/kernel_heap/`, `cargo xtask qemu`, `docs/releases/v0.28.0-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.35.0-rc.md`, `docs/releases/v0.35.2-rc.md`, `docs/releases/v0.35.4-rc.md`, `docs/releases/v0.35.5-rc.md`, `docs/RELEASE_PLAN.md` v0.33.1 |
| Descriptor tables | Early GDT/TSS setup remains single-core and uses an uninitialized/in-progress/ready init guard; FS/GS selectors plus admitted FS/GS/KERNEL_GS base MSRs are cleared before future TLS/per-CPU policy exists; future `set_ring0_stack()` callers must provide a valid current-CPU kernel stack and mask interrupts before mutating the global TSS `rsp0` field, with release-mode validation of the interrupt-masking contract and a single aligned 64-bit store until per-CPU TSS storage exists | Active candidate | `crates/aesynx-arch-x86_64/src/descriptors.rs`, `docs/releases/v0.24.0-rc.md`, `docs/releases/v0.32.0-rc.md` |
| Advanced address-space hardening | Scalable frame-indexing, huge-page leaves, PCID/INVPCID invalidation, SMEP/SMAP/UMIP enforcement, Spectre-class controls such as IBRS/STIBP/SSBD/ARCH_CAPABILITIES, CET/shadow-stack support, optional PKU/PKS compartments, confidential-computing memory attributes, LAM-style pointer-tag fast paths, and property/model-checked mapper invariants must be added only with explicit hardware capability checks, fail-closed fallbacks, and release-gated tests | Planned; mapper properties active candidate | `cargo xtask fuzz-smoke`, `docs/memory-model-roadmap.md`, `docs/RELEASE_PLAN.md` |
| Early entropy semantics | Identifier generation distinguishes anti-confusion monotonic generations from attacker-unpredictable random tokens; `aesynx-entropy` classifies deterministic fallback as anti-confusion only, generation counters fail instead of wrapping, random-token policy rejects deterministic fallback, CPUID-only hardware evidence, and raw hardware entropy without a DRBG self-test, x86_64 `RDRAND`/`RDSEED` support is CPUID-classified as future seed material rather than a sole trust anchor or direct token source, and QEMU reports only redacted entropy booleans before `[TEST] entropy-policy=ok`; raw entropy and token material must never be logged | Tagged; DRBG gate active | `aesynx-entropy`, `aesynx-arch-x86_64::entropy`, `docs/releases/v0.18.1-rc.md`, `docs/releases/v0.35.0-rc.md` |
| Entropy token consumption | Future entropy-backed token or capability-id generation must use `require_random_tokens()` as the policy gate; direct `primary_source` reads remain telemetry/classification only until a lint or equivalent test enforces the call pattern; until a runtime self-test-backed hardware read path and DRBG self-test exist, security-sensitive consumers must fail the operation or boot rather than silently accepting deterministic fallback or raw hardware output; `random_tokens_available=false` is the expected production state while the kernel smoke reports `drbg_self_test=false`, and AP startup or future security-sensitive consumers must not introduce attacker-unpredictable token use before a DRBG implementation is wired; any hardware entropy path must include a runtime self-test that detects stuck or repeated sample patterns rather than relying only on CPUID or instruction success; v0.18.2 tracks the concrete no_std DRBG, seed-combination policy, KAT/startup self-tests, reseeding, per-core domain-separated streams, zeroization, and no-copy migration/snapshot rules | Planned; DRBG gate active; implementation blocker tracked | `aesynx-entropy`, `docs/releases/v0.22.0-rc.md`, `docs/releases/v0.32.0-rc.md`, `docs/releases/v0.35.0-rc.md`, `docs/releases/v0.35.2-rc.md`, `docs/RELEASE_PLAN.md` v0.18.2, v0.35.2 |
| Object-native memory | Allocations and mappings should grow toward explicit owner, purpose, lifetime, sensitivity, sharing, snapshot, DMA, and revocation policy instead of anonymous ambient memory | Planned | `docs/memory-model-roadmap.md` |
| OS world model | Kernel and services emit bounded facts; native userspace world services provide policy-aware queries, context packs, projections, and AI-safe explanations without creating authority | Planned | `docs/os-world-roadmap.md`, `docs/userspace-vision.md` |
| Live OS world service | `worldd` must ingest/query facts through capabilities, validate source identity/provenance, apply redaction before joins, enforce CPU/memory/result budgets, preserve restart reconstruction and completeness frontiers, and never treat absence of telemetry as authoritative without a completeness certificate; long-running queries use bounded live read leases or snapshot authorization, observe cancellation/freeze, revalidate authority and classification before result publication, prevent revoked rows from being newly released, partition caches by authorization/classification, prevent replay across principals, and free bounded state on cancellation | Planned | `docs/RELEASE_PLAN.md` v0.60.2, `docs/os-world-roadmap.md` |
| Early diagnostics | Structured log-level records with validated components, escaped and bounded panic output, basename-only source location, boot phase, early core, message, arithmetic-only RFLAGS, redacted register summary, and QEMU panic marker | Active candidate | `aesynx-kernel::diagnostics`, `aesynx-arch-x86_64::registers`, `cargo xtask qemu --panic-smoke` |
| Kernel codegen boundary | Bare-metal kernel builds disable SSE/AVX until explicit FPU/SIMD context switching exists | Active candidate | `.cargo/config.toml`, `tools/xtask/src/kernel_flags.rs`, `cargo xtask build-kernel` |
| x86_64 descriptor tables | Early boot installs a private GDT through an uninitialized/in-progress/ready guard, reloads CS/SS/DS/ES, nulls FS/GS selectors, clears admitted FS/GS/KERNEL_GS base MSRs, loads TSS, and reserves a dedicated double-fault IST stack before generic kernel work | Active candidate | `aesynx-arch-x86_64::descriptors`, `cargo xtask qemu` |
| x86_64 exception tables | Early boot installs a private IDT through an uninitialized/in-progress/ready guard with halt-and-log catch-all stubs for every vector before overriding breakpoint, page-fault, and double-fault entries; invalid raw exception-frame pointers fail-stop instead of returning toward `iretq`; interrupt gate installation is crate-private, boot/smoke-scoped, and uses a release/debug-identical mask-and-restore error path while a future NMI exclusion strategy remains required before general live IDT mutation; page-fault smoke prints CR2 presence/page offset, CR3 low bits, RFLAGS summary, interrupt state, and decoded error bits; generic unhandled exception diagnostics report typed error-code classes rather than raw selector/error-code values | Active candidate | `aesynx-arch-x86_64::exceptions`, `cargo xtask qemu --exception-smoke`, `docs/releases/v0.35.0-rc.md` |
| x86_64 interrupt baseline | Legacy PIC is remapped out of exception vectors before masking; local APIC is detection-only until MMIO mapping exists; IRQ vectors and EOI paths are checked | Active candidate | `aesynx-arch-x86_64::interrupts`, `cargo xtask qemu` |
| x86_64 timer smoke | Opt-in PIT IRQ0 smoke installs a checked vector 0x20 handler, enables interrupts only for the controlled test, acknowledges each tick, and disables IRQ0 after the fixed three-tick target | Active candidate | `aesynx-arch-x86_64::timer`, `cargo xtask qemu --timer-smoke` |
| Monotonic time and sleep queue | Timer ticks convert into checked monotonic instants using the timer-provided rate; bounded sleep requests wake only when due and prefer the earliest due deadline; timeout checks fail closed on overflow; current fixed-capacity sleep queues use linear scans and timer/IRQ call sites must keep `N` small or move to sorted/heap-backed deadline tracking before high-capacity use | Active candidate | `aesynx-time`, `cargo xtask qemu --timer-smoke`, `docs/releases/v0.32.0-rc.md` |
| Telemetry integrity | Task telemetry uses append-only counters including scheduled-run counts; core telemetry snapshots are advisory per-counter samples, capability faults can be recorded as redacted kind-plus-count events without cap/object IDs, `inc_cap_faults` propagates the same overflow error as `record_cap_fault`, scheduler decision records and scheduler event payloads redact task IDs in `Debug`, bounded queue-depth fields expose saturation state instead of silently hiding overflow, versioned event headers are stamped by the per-core ring, scheduler-facing AI features are produced through bounded redaction adapters rather than raw counters, trace export has a central CoreId policy plus tests proving raw task IDs cannot bypass redacted event output, and AI policy inputs remain bounded fixed-point features rather than raw telemetry snapshots | Active candidate | `TaskTelemetry`, `CoreTelemetry`, `SchedulerTelemetry`, `PerCoreEventRing`, `record_cap_fault`, `redacted_schedule_features`, `tools/trace-decode`, `aesynx-ai-policy`, `docs/releases/v0.32.0-rc.md` |
| Concurrency discipline | Aesynx uses SMP/APIC mechanisms for x86_64 multicore bring-up, but the intended architecture is software-defined AMP/multikernel: per-core ownership, explicit core roles, directed IRQ ownership, bounded messages, and narrow documented shared-state boundaries rather than a classic shared-everything SMP kernel; production cross-core shared locks are not an accepted mechanism for mutable OS state, and unavoidable bootstrap/hardware-control exceptions need release-note justification plus a migration story to owner-core messages, replicated protocols, or capability-scoped shared buffers; unavoidable multicore work must have IRQ-safe locking, interrupt-disable guards, lock-ordering rules including the `CoreTopology` rank, blocking/allocation/logging rules, and a replacement for current single-core static GDT/TSS/IDT/kernel-heap/activation-storage mutation before shared state is relied on; `aesynx-sync` provides safe early lock guards, software-model interrupt guards, nested interrupt masking semantics, non-LIFO guard-release poison semantics, IRQ-lock composition, lock-rank validation, and QEMU evidence before `[TEST] concurrency=ok`; `try_lock_irq()` requires a linear non-`Clone`/non-`Copy` `ArchIrqDisableProof` argument so the software model cannot be mistaken for hardware masking, the temporary model-only constructor is repository-policy confined to the audited single-core smoke/test context, and an arch-backed token remains required before IRQ-handler use; service queues must scrub vacated shared slots before cross-domain observation; the kernel `smp` feature currently propagates to the x86_64 arch crate and compile-errors as a tripwire until the documented migrations exist | Tagged; SMP hardware tripwire active | `aesynx-sync`, `crates/aesynx-kernel::concurrency_smoke`, `docs/concurrency-policy.md`, `docs/releases/v0.33.1-rc.md`, `docs/releases/v0.35.0-rc.md`, `docs/releases/v0.35.4-rc.md`, `docs/releases/v0.35.5-rc.md`, `crates/aesynx-kernel` and `crates/aesynx-arch-x86_64` `smp` features |
| AMP core model | Core identity, role, heterogeneous capability metadata, local state, owner-scoped registries, local telemetry, and sealed boot barriers are modeled in safe no_std Rust before multicore startup; the bootstrap core is represented as `CoreLocal` with bootstrap role, x86_64/control capability metadata, registry entry, telemetry, and boot-barrier arrival before `[TEST] amp-core=ok`; this is model evidence only and does not enable secondary cores, AP startup, per-core descriptor tables, IRQ routing, or cross-core fabric messaging | Tagged; single-core evidence only | `aesynx-core`, `crates/aesynx-kernel::amp_core_smoke`, `docs/releases/v0.34.0-rc.md` |
| AP startup state table | QEMU smoke paths run with `-smp 4` from the shared `aesynx-core::QEMU_MULTICORE_TOPOLOGY_CORES` constant, generated manifests record `qemu_smp_cpus=4`, and `aesynx-core::CoreTopology` models four discovered cores with separate hardware state and role-assignment state before `[TEST] multicore-topology=ok`; topology, registry, and boot-barrier setup mutation is owner-scoped, role assignment is rejected after online, staged cores receive non-forgeable `CoreStartupTicket` values, no public ticket-discarding startup wrapper remains, `CoreStartupArrival` evidence can only be derived from a matching ticket, startup evidence remains non-`Sync` so it cannot become a shared cross-core snapshot object by accident, hardware-online transition consumes arrival evidence by value, validates target core, hardware ID, coordinator, and exact staged startup epoch, rejects stale or future epoch evidence before mutation, evaluates core/hardware mismatches without short-circuiting, keeps raw startup epochs private and debug-redacted, mismatch errors carry no associated topology data, clears startup evidence fields through fenced zeroing stores on drop, direct online-without-startup is unrepresentable through the public API, quarantine is reachable only from discovered/offline or startup-staged/booting state until a real watchdog/fault path exists, quarantined cores are excluded from active assignment and role aggregate counters, unassigned discovered cores are not counted as idle-role assignments, the internal topology epoch is not exposed as public status, duplicate logical core IDs and duplicate hardware IDs fail closed, and failed state transitions leave topology state unchanged; AP launch preflight accepts only staged/booting entries, requires owner-scoped mutation, caller-supplied `ApStackRegion` bounds, non-overlapping page-aligned stack ranges of at least 32 KiB, unique hardware IDs, and nonzero watchdog ticks before mutation, treats shared-bootstrap-only descriptor tables as an explicit execution blocker surfaced by `ap_execution_blocked_ok=true`, and can mint a sealed non-`Copy` dispatch token only for the topology owner when execution is allowed; QEMU proves the current shared-bootstrap descriptor state blocks token minting with `ap_dispatch_token_blocked_ok=true`; topology mutation validates the explicit `CoreHardwareState` x `CoreAssignmentState` x `CoreState` table before mutation and revalidates after mutation, QEMU reports `state_table_ok=true`, the future APIC INIT/SIPI path must consume the sealed dispatch token instead of consulting advisory state after the fact; future AP execution must carry startup-attempt generations, late-arrival quarantine, APIC-ID reuse detection, topology snapshot epochs, boot-parameter publication/consumption barriers, and read-only or zeroed AP parameter pages after consumption; the existing `smp` compile-time tripwires remain because AP execution, per-core descriptor tables, directed IRQ routing, and cross-core messaging are still non-goals for this release | Active candidate; dispatch-token gate only, AP execution still blocked | `aesynx-core::CoreTopology`, `CoreStartupTicket`, `CoreStartupArrival`, `ApStartupPreflight`, `ApStartupDispatchToken`, `crates/aesynx-kernel::multicore_topology_smoke`, `tools/xtask/src/image.rs`, `docs/releases/v0.35.2-rc.md`, `docs/releases/v0.35.3-rc.md`, `docs/releases/v0.35.4-rc.md`, `docs/releases/v0.35.5-rc.md` |
| Firmware topology normalization | Future AP startup, IRQ routing, and NUMA policy must consume normalized kernel-owned topology data rather than raw mutable firmware tables; ACPI/MADT input requires RSDP/XSDT/RSDT provenance and bounds checks, table length/checksum/revision/alignment/overflow validation, bounded subtable walking, unknown-entry skip rules, duplicate/conflicting APIC rejection, BSP and disabled-CPU policy, NMI/source-override normalization, APIC/x2APIC consistency, SRAT/SLIT consistency, fuzzing, truncation tests, and differential parser fixtures | Planned | `docs/RELEASE_PLAN.md` v0.35.4.1 |
| Input devices | Early interactive input should use serial or virtio-serial first, optional PS/2 keyboard/mouse for local QEMU input, and USB HID only after xHCI; input endpoints must be capability-scoped, queues must reject overflow without leaking stale bytes, diagnostics must not log raw keystreams by default, and PS/2 or USB drivers must not receive broad port/IRQ/DMA authority | Planned | `docs/RELEASE_PLAN.md` v0.44.2, `docs/RELEASE_PLAN.md` v0.44.3, `docs/driver-roadmap.md` |
| User memory access | Ring-3 and ABI work must use checked copy-in/copy-out helpers, copy-then-validate syscall rules, page-permission checks, SMAP access-window design, and contained user-copy faults; hostile copy-in is all-or-nothing with initialized destination snapshots, incomplete copies discarded before parsing, no shadow-cursor advance, no authority action from partial snapshots, bounded ingress work budgets, and kernel-owned pending completions on copy-out failure; `VirtAddr::new_x86_64_canonical` centralizes the first x86_64 canonical-address check for future boundary code | Planned | `docs/RELEASE_PLAN.md` v0.44.4, `aesynx-abi::VirtAddr` |
| Native syscall and IPC ABI | Ring-3 syscall dispatch requires a fixed-width Aesynx-native ABI with architecture-neutral request/response semantics separated from x86_64/aarch64/RISC-V entry conventions, exact per-arch register convention, stack alignment, version/feature negotiation, syscall and endpoint operation IDs, explicit true-syscall versus RPC split, caller-table-incarnation capability interpretation, no Rust/compiler-dependent layout, rejection of unknown mandatory flags before mutation, bounded work per invocation, copy-in/out ownership rules, blocking-call transaction states, interruption/retry semantics that cannot repeat committed operations, cross-architecture golden vectors, and no POSIX ambient namespace assumptions | Planned | `docs/RELEASE_PLAN.md` v0.46.4, `docs/userspace-vision.md` |
| User fault delivery | User exceptions require deterministic fatal or capability-authorized endpoint delivery policy, redacted telemetry records separate from authorized exception disclosure to a handler with explicit authority over the target address space, one-shot frame-generation-bound resume capabilities for recoverable handlers, separate inspect/modify/map/resume rights, recursion limits, guarded handler-stack policy, failed/full endpoint fallback to teardown, debugger capabilities scoped to explicit target domains, and budget charging for repeated malicious faults | Planned | `docs/RELEASE_PLAN.md` v0.46.5 |
| Initial task/domain model | First native userspace uses one task per domain: exit terminates the domain, fatal task faults initiate domain teardown, exception/pager handlers normally live in external service domains, no task-create/thread-create/join syscall exists, and TLS has exactly one instance per initial domain until the later multi-task-domain gate lands | Planned | `docs/RELEASE_PLAN.md` v0.46.6 |
| Userspace loader and lifecycle | Native executable loading, ASLR, spawn, and teardown require sealed executable-source hashes, canonical signed/hash-bound load manifests, per-domain executable image-instance identity for relocated bytes, image-instance lineage through generic cross-object dependency edges, bounded canonical load plans, confined ELF parsing, kernel-side mapping invariant validation, executable transition staging from private RW/NX through image-instance sealing to final RX, no RW/RX physical aliases across address spaces, architecture-specific instruction-cache synchronization, transactional segment installation, user ASLR from domain-separated DRBG labels, typed domain factory/control/debug capabilities, transactional domain spawn with explicit initial capabilities and quota escrow, creator identity not acting as authority, multi-task domains gated behind explicit thread-create authority after single-task userspace evidence, distinct wait/read/consume result-capability semantics, and domain termination that fences execution, revokes authority, drains resources, unconditionally sanitizes plaintext private memory before cross-domain reuse, bounds exit records, and changes incarnation before reuse | Planned | `docs/RELEASE_PLAN.md` v0.47.2, v0.47.3, v0.47.4, v0.51.2 |
| User TLS and startup block ownership | `aesynx-rt` must define whether TLS is unavailable or provide a startup/TLS layout; user FS base is canonical/range checked, user GS is prohibited or reserved when kernel GS is used, TLS base is saved/restored across context switch and migration, FSGSBASE is disabled/trapped/validated by policy, teardown resets TLS, TLS descriptors cannot target kernel or another domain, and startup blocks are kernel-owned, read-only, offset-based, bounded, zero-padded, table-local, generation-stamped, and never contain kernel pointers, physical addresses, raw object IDs, KASLR data, or other-domain handles | Planned | `docs/RELEASE_PLAN.md` v0.47.0 |
| Capability revocation | REVOKE authority check required before epoch mutation; epoch and generation counters must fail rather than wrap; v0.37.0 couples cross-table IPC revoke evidence to the object registry revocation epoch so receiver-table capabilities for the same object fail live resolution after revoke; future production multi-core revoke still needs a broadcast or agreement protocol for distributed tables | Active candidate | `ensure_revoke_authority`, `RevocationEpochStore`, `ObjectRegistry::increment_epoch`, `Capability`, `crates/aesynx-kernel::capability_ipc_smoke`, `docs/releases/v0.37.0-rc.md` |
| Early serial safety | COM1 output uses admitted ports, bounded polling, and a single-core marker type | Active for boot | `crates/aesynx-arch-x86_64/src/serial.rs`, `crates/aesynx-arch-x86_64/src/port.rs` |
| Drivers | Separate driver tree/packages, signed driver manifests, MMIO/IRQ/DMA caps, isolated driver services, and revocation lifecycle | Planned | `docs/driver-roadmap.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Directed IRQ ownership | IRQ sources and vectors require incarnations, exactly one owner core/domain, mask-drain-reroute-ack-unmask transfer, EOI and device-clear ordering, MSI/MSI-X and interrupt-remapping teardown, stale interrupt rejection after restart, storm limiting, and special policy for NMI, machine-check, timer, and non-routable interrupts | Planned | `docs/RELEASE_PLAN.md` v0.41.1 |
| IOMMU and interrupt revocation | Production DMA-capable driver revocation must disable bus mastering, reset or function-level-reset devices where available, unmap IOMMU entries with completed IOTLB invalidation, invalidate or prohibit ATS/PASID/PRI, fence interrupt-remapping/MSI/MSI-X delivery, use device and interrupt incarnations, apply device-memory ordering barriers around bus-master disable, reset, IOTLB completion observation, interrupt-remap teardown, and queue teardown, and fail closed for devices that cannot be reliably quiesced or fenced | Planned | `docs/RELEASE_PLAN.md` v1.2, `docs/driver-roadmap.md` |
| Developer SDK | App developers must use stable `aesynx-abi`, `aesynx-rt`, target specs, app/package manifests, and explicit capability handles rather than kernel-private headers or ambient authority; `aesynx-rt::Env` exposes capability handles through read-only accessors while redacting all capability identifiers from debug output | Planned; Env redaction active | `docs/sdk-roadmap.md`, `docs/userspace-vision.md`, `docs/RELEASE_PLAN.md` v0.47.1, `crates/aesynx-rt`, `docs/releases/v0.32.0-rc.md` |
| Native shell | `aesynx-shell` currently declares names and builtins only; future parser/dispatcher work must capability-gate authority-bearing builtins such as reboot, capability/object inspection, and logging, and must surface telemetry through redacted summaries rather than raw kernel structures | Planned; stub only | `crates/aesynx-shell`, `docs/userspace-vision.md`, `docs/releases/v0.32.0-rc.md` |
| WASM | Sandboxed extension model | Planned | `docs/userspace-vision.md` |
| AI | Bounded advisory policy interface, deterministic fallback, and non-AI scheduler heuristic evidence | Tagged | `aesynx-ai-policy`, `crates/aesynx-kernel::ai_policy_smoke`, `docs/releases/v0.33.0-rc.md` |
| QEMU testing | Boot smoke validates GDT/TSS/IDT setup, breakpoint handling, BootInfo, memory-map accounting, bounded frame-allocator smoke, bounded page-table mapper smoke, every v0.16 kernel mapping policy status boolean, kernel-owned CR3 activation, kernel stack guard evidence, CPU hardening read-back evidence, v0.18.1 entropy policy evidence, v0.18 kernel heap slab/page-run allocation/free evidence, invalid-free and double-free telemetry, v0.20 capability-table lifecycle evidence, v0.21 memory-capability mapping-descriptor evidence, v0.22 capability audit/cap-fault telemetry evidence including mint/derive/grant/revoke coverage, v0.26 kernel service-queue request/completion evidence including unsupported-service pending-count denial, v0.27 task-model evidence, v0.28 cooperative executor evidence, v0.29 scheduler telemetry evidence, v0.30 telemetry event schema evidence, v0.31 trace-event export evidence, v0.33 AI policy heuristic/fallback evidence, v0.33.1 concurrency discipline evidence, v0.34 AMP core evidence, v0.35 QEMU `-smp 4` topology evidence, v0.36 pairwise IPC fabric evidence, v0.37 capability grant/revoke-over-IPC evidence, and Rust serial markers; generated release manifests reject ASCII control characters, Unicode line/paragraph separators, and `=` in dynamic path/version fields before writing evidence; panic smoke validates early panic diagnostics; exception smoke validates page-fault decoding; timer smoke validates three controlled PIT IRQ0 ticks plus delayed sleep wake markers; CI runs the full smoke suite | Active for boot | `cargo xtask qemu-suite`, `cargo xtask qemu`, `cargo xtask trace-decode`, `cargo xtask qemu --panic-smoke`, `cargo xtask qemu --exception-smoke`, `cargo xtask qemu --timer-smoke`, `docs/RELEASE_PLAN.md` |
| Future bootloader | Rust UEFI bootloader must stay a minimal signed/measured security gateway with crypto-agile capsule verification, TPM measurement where available, enforced anti-rollback counters for production profiles, and explicit recovery-key policy before verified-boot enforcement claims | Planned | `docs/bootloader-roadmap.md` |
| Bootloader response pointers | Limine response references must be non-null, aligned, and higher-half kernel VMA pointers before Rust references are created; framebuffer, HHDM, and RSDP payload addresses are checked separately before entering BootInfo | Active candidate | `crates/aesynx-kernel/src/limine.rs`, `cargo xtask qemu` |

## Admission Rule

Security-sensitive features should not graduate from planned to active until
they have:

- Tests or model checks.
- Documentation.
- Failure-mode analysis.
- Release-gate coverage.
- Clear non-claims.
